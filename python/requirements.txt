# Optional dependencies for running with FastAPI or Flask
# Note: python/app.py runs directly with Python 3 standard library (no pip install needed!)

fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
flask>=2.3.0
requests>=2.31.0
supabase>=2.0.0
python-dotenv>=1.0.0
